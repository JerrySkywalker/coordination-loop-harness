# {{PROJECT_NAME}}

This coordination repository was bootstrapped from
`{{TEMPLATE_REPOSITORY}}` version `{{TEMPLATE_VERSION}}`.

Use it as a durable, human-supervised coordination mailbox. Raw evidence,
credentials, local leases, and generated local state do not belong in Git.
